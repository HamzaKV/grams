import { isAuthenticated } from './auth';
import { list } from './list';
import { user, firstName, lastName, email } from './user';

export default {
    isAuthenticated,
    list,
    user,
    firstName,
    lastName,
    email,
};
